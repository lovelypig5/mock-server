import {
    StyleSheet
} from 'react-native';

const layout = StyleSheet.create({
    title: {
        color: '#FFF',
        justifyContent: 'center'
    },
    button: {},
    text: {
        color: '#EEE'
    }

});

export default layout;
