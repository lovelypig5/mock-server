const getters = {
    alert(state) {
        return state.alert;
    },
    modal(state) {
        return state.modal;
    },
    user(state) {
        return state.user;
    },
    lang(state) {
        return state.lang;
    }
}

export default getters;
